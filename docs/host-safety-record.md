# Protected-host safety record

This tracked record contains sanitized aggregate conclusions only. Raw commands, host identity, users, addresses, topology, service lists, configuration, telemetry, credentials, API responses, logs, VM paths, and measurements remain in ignored private evidence.

## 2026-07-30 implementation baseline

| Gate | Aggregate result |
| --- | --- |
| Repository/worktree safety | Passed; local agent/operator/runtime paths remained ignored and no private candidate file was tracked |
| Build/unit/contract baseline | Passed; the final isolated user-owned PostgreSQL window ran all 366 tests with no skips plus fresh-database schema apply/validation |
| Initial current-host observation | Read-only baseline recorded; existing SIEM agent left running during that validation phase; no config content read, privilege broadened, or product service restarted |
| Current-host bounded canary | Passed with a short-lived unprivileged foreground agent, five-or-fewer L1 records per poll, private ignored queue/state/database, and loopback HTTPS; 72/72 unique records and 13 heartbeats arrived, L1 was healthy/acknowledged, and the private queue drained to zero |
| Initial current-host liveness closeout | Existing agent remained active/enabled with zero restarts after the foreground canary; the already-running API answered a certificate-pinned health probe; no installed service, configuration, permission, group, trust store, or startup setting was changed during that phase |
| Authorized agent replacement attempt | The operator later explicitly authorized uninstalling the prior agent and installing the exact VM-validated 2.0.2 bundle. A stopped-state rollback archive, queue integrity check, exact hashes, configuration/trust preservation, service/drop-in inventory, and rollback procedure were verified first |
| Protected-host compatibility gate | The 2.0.2 service installed and started with its expected hash and no restart loop, but the current backend exposed v1 routes only. Its required `/api/v2` routes were absent, so delivery could not be acknowledged and the rollout failed closed despite generic backend health |
| Protected-host rollback | Passed; the candidate was stopped, its unsent durable state was preserved privately, and the exact prior binary, configuration, CA, queue/state, unit, and drop-ins were restored. The v1 service returned active/enabled with one process and zero restarts, its queue reached zero and drained a later bounded burst, poison remained empty, SQLite integrity was healthy, and v1 heartbeat/inventory/ingestion received successful responses |
| Authorized parallel v2 cutover | Passed after explicit operator approval; a fresh schema-v2 database, separate least-privilege identity, new credentials, private CA, and loopback-only parallel 2.0.2 API were created without modifying the v1 database or stopping its writer during prevalidation |
| Protected-host v2 installation | Passed; the exact VM-validated 2.0.2 agent replaced the restored v1 agent only after a root-only stopped-state archive and empty healthy queue were verified. The prior checkpoints/state and approved passive-telemetry posture were preserved, L4 remained disabled, enrollment rotated to a per-agent credential, and the new queue drained to zero with no poison rows or restarts |
| Protected-host v2 data path | Passed; live heartbeat, inventory, three event source families, source health, PostgreSQL storage, detection alerts/evidence, service-authenticated REST, bounded read-only MCP, redaction, and MCP audit records were observed. The service reports the host honestly at L1/degraded against an L2 target because optional/permission/unsupported gaps remain |
| Authorized 2.1.0 protected-host upgrade | Passed; the exact final VM-regressed agent/server artifacts were installed after private rollback capture and a drained healthy queue. L4 was disabled for installed-identity preflight, the prior L4 state was retained privately rather than deleted, and a clean exact baseline plus plan was reviewed before activation |
| Protected-host full implemented coverage | Passed; the reviewed roles reached strict L4/healthy after the complete SLO window, including two consecutive steady-state server assessments. Mandatory gaps, degraded/error/permission-denied sources, active L4 gaps, drops, and poison were zero; the local queue drained to zero. The optional Audit Framework source remains explicitly unsupported and undeclared role packs remain not applicable |
| Authorized 2.2.0 protected-host upgrade | Passed for exact-artifact activation and bounded recovery. API-first activation, private pre-2.2 and recovered-L3 archives, drained-queue gates, candidate-first lifecycle plans, fresh passive/self-integrity/L4 approvals, restart-always packaging, socket ownership, paging/interfaces, disk diagnostics, liveness recovery, retention scheduling, and audit-disabled routing were verified. A concurrent-state defect discovered during preflight caused activation to stop; the queue and checkpoints were preserved, fixes were added and VM-regressed, and final replay reached zero queue/poison without a reset or manual row deletion. |
| Protected-host 2.2.0 current coverage | Short gate passed; long gate pending. Heartbeats and monitor execution are fresh, the current host heartbeat-loss alert is resolved without disposition or closure, latest inventory generations are complete, self-integrity/network/metrics sources are healthy, and audit parsing remains disabled and undeclared with no audit transport in L1. Executable-link readability remains below the required process threshold. The first full L4 window stayed below CPU/RSS limits but breached its write-rate guardrail during rollout/replay; seven-day history is not ready, and post-validation recovery plus the 24-hour/seven-day follow-ups remain mandatory. |
| Authorized 2.4.0 process-visibility upgrade | Passed. The operator explicitly prioritized complete telemetry and approved the separately planned fixed profile. A stopped-state private rollback archive preceded installation; the agent remained non-root with only `CAP_SYS_PTRACE`, no `CAP_SYS_ADMIN`, `NoNewPrivileges`, the existing service sandbox, the fixed direct-inspection syscall deny list, and self-integrity coverage of the drop-in. Normal install/upgrade did not grant the capability implicitly. |
| Disposable-VM privilege and rollback gate | Passed. The exact profile produced both readability ratios above 80%, retained bounded invalid-text enrichment as healthy partial evidence, and blocked a direct ptrace attempt through the service syscall filter. Removing the exact drop-in and restarting only the guest agent removed the effective capability and returned cross-user executable coverage to degraded. The guest was then shut down, reverted to its pre-test snapshot, and left powered off with autostart disabled. |
| Protected-host 2.4.0 full implemented coverage | Passed; long soak pending. The process source recovered healthy with no active gap or core malformed evidence while cumulative and legacy counters remained intact. The documented disabled L4 cleanup removed only prior L4 state; a new installed-state baseline and plan were privately approved. After the full 15-minute window, all applicable mandatory sources were healthy, strict L4 was attained, queue and poison were zero, and the four affected high-severity process/policy detection prerequisites were satisfied. |
| Retained v1 plane | Passed; after v2 validation the v1 writer was stopped and disabled, its database had zero sessions and reports read-only defaults for the database and former application role, and its integrity-listed dump plus private runtime/configuration/TLS/agent rollback assets remain retained |
| Host mutation | Only explicitly approved SIEM product files/service lifecycle and one bounded pacman evidence transaction changed. The single probe package was installed then removed; the exact pre-test package-name set and count were restored. None occurred to boot, kernel, initramfs, disks, mounts, firewall, routing, DNS, authentication, accounts/groups, SSH, audit, capabilities, journal permissions, or unrelated startup policy |
| Virtualization preflight | Passed using the already-installed libvirt/KVM stack; no package, host service, or network change required |
| Disposable VM | Small Debian guest created with VM-only credential, NAT, sparse disk, autostart disabled, and clean pre-SIEM snapshot; boot recovery passed; guest shut down between test windows |
| Host resource gate | Passed for each bounded VM window; one earlier attempt stopped before launch below the predeclared threshold, and later windows began only after headroom recovered |
| Isolated data-plane validation | Passed using a Unix-socket-only user PostgreSQL cluster and short-lived APIs restricted to loopback or the existing libvirt NAT bridge; bridge-bound API stopped after VM validation |
| Guest lifecycle and rollback | Passed; exact-2.1.0 install/upgrade, group-gated L1-L4 visibility, reviewed L3/L4 activation, reboot, outage, pressure, uninstall, post-uninstall reboot, parser regression, and clean-snapshot revert were confined to the exact disposable guest; guest ended shut off |
| Current-host reboot | Not required and not authorized |

## Change/rollback boundaries

- Repository changes affect only project code, tests, schemas/contracts when explicitly listed, and public synthetic documentation.
- The disposable VM affects only its exact libvirt domain, exact qcow2 volume, and ignored VM evidence. Recovery is the clean baseline snapshot or recreation; teardown must verify exact targets first.
- The protected-host 2.x retry was subsequently approved and completed through a parallel fresh-database cutover. The v2 database is the active writer; the retained v1 database remains a separate read-only rollback/validation asset. No in-place 1.x schema migration was attempted.
- The operator explicitly approved the current journal scope, live L2/L3/L4 activation, and the optional 2.4.0 cross-user executable-visibility profile. Exact self-integrity, passive/socket-ownership/process-visibility, posture-baseline, audit, and L4 approvals remain configuration-bound; changing their inputs invalidates approval rather than silently broadening collection. The one-capability profile is scoped to telemetry collection, monitored by self-integrity, and removed through its exact separately reviewed rollback path.
- The guest-only `systemd-journal` membership and private test CA were removed during uninstall verification and were absent after snapshot revert. No host trust store, group, ACL, journal setting, or certificate was changed.
- The protected-host canary trusted its one-day test certificate only through the foreground process environment. It did not install a CA, write a unit, emit a synthetic journal message, restart the installed agent, or alter journal visibility. Its exact API and PostgreSQL processes were stopped after the queue drained.
- The later lifecycle attempt reused the already installed private CA and approved agent configuration; it did not alter the host trust store or print credentials. The failed candidate queue and rollback archives remain only in ignored, access-restricted evidence. Because the v2 backend never accepted the candidate's records, that short candidate collection window is retained as an explicit delivery gap rather than silently discarded or converted for v1.

Stop and roll back on privacy leakage, unauthorized mutation, host impact, boot/network/authentication regression, queue/state corruption, silent loss, uncontrolled growth, false-healthy coverage, or inability to recover the guest.
