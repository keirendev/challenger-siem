# Changelog

All notable project changes should be recorded here.

## Unreleased

- **PATCH 1.11.4:** Fixed `linux-kernel-security` quiet-period health so a current successful bounded system-journal observation, rather than the age or presence of rare kernel-security activity, establishes source freshness. `kernel_security`, `security_module`, and `kernel_module` retain independent observed/not-observed evidence and a quiet observation does not satisfy a detection's recent-event requirement. Expired or implausibly future observations, unavailable or degraded prerequisites, permission denial, and active continuity gaps remain explicit fail-closed states. The change preserves `/api/v1` and `contracts/v1`, requires no database migration, and uses synthetic aggregate validation only.
- **PATCH 1.11.3:** Resolved ambiguous `linux-package-management` applicability by deriving aggregate producer evidence from the existing bounded read-only dpkg/rpm/pacman inventory, while retaining a distinct degraded quiet state until a supported package-change journal record proves visibility. Supported, quiet, known out-of-scope, missing, denied, timed-out, and malformed inventory states now map deterministically to applicability, prerequisite, and per-family evidence; the producer matrix and synthetic-only validation boundary are documented. The change preserves `/api/v1` and `contracts/v1`, performs no install/update/removal, reads no package-manager log, and includes no private host evidence.
- **PATCH 1.11.2:** Fixed `linux-agent-log-tamper` quiet-period health so a current successful bounded journal observation, rather than the age or presence of rare `agent_tamper` and `log_tamper` activity, establishes freshness. The two families retain independent observed/not-observed evidence, while expired or implausibly future observations, missing prerequisites, permission denial, and active gap/clear/bookmark conditions remain explicit non-healthy states. The change preserves `/api/v1` and `contracts/v1`, requires no database migration, and does not generate production activity or weaken visibility-loss reporting.
- **PATCH 1.11.1:** Removed the web-console header API/contract/coverage badges, sidebar authorization note, and displayed operator identity/role while keeping the authenticated sign-out action and responsive shell layout intact. Authentication, authorization, API, contract, and coverage behavior are unchanged.
- **MINOR 1.11.0:** Added bounded read-only Arch Linux and derivative package coverage through fixed pacman installed-package and available-update inventory operations, including an explicit silent-empty update result that does not hide stderr failures, plus pacman/ALPM journal classification for genuine package changes. The controlled upgrade helper can now validate and preserve an explicitly selected installed mode-0600 configuration without attempting a same-file copy, and runs the non-login-identity L4 preflight from its owned private state directory instead of inheriting an inaccessible operator workspace. The capability preserves `/api/v1` and `contracts/v1`, does not install or update packages, does not read package-manager log files, and keeps journal coverage degraded until a real matching record is observed.
- **PATCH 1.10.2:** Stabilized local development endpoint ownership by centralizing fixed workflow ports, allowing the platform lifecycle helper to delegate to one persistent user-systemd service, requiring explicit Kestrel certificate files for helper-managed HTTPS, supporting a private CA for health checks, and aligning the direct launch profile with the documented port. This prevents a competing development process from silently replacing the certificate used by an enrolled local Linux agent while preserving disposable smoke and approved Windows-lab listener isolation.
- **PATCH 1.10.1:** Fixed the `soc-agent` conversation grid so hiding the transient live-connection banner after a response cannot displace, collapse, or clip the chat composer; the transcript and composer now retain explicit rows while the internal transcript scrolling and `/api/v1` contracts remain unchanged.
- Hardened public-repository exclusions for local Codex and other AI coding-assistant state while retaining only the intentional server-side ChatGPT subscription integration in tracked application code.
- **MINOR 1.10.0:** Rebuilt the authenticated `soc-agent` workspace around a dominant chat window, a narrow recent-chats sidebar, a compact header, and a focused sticky composer, with detailed tool activity deliberately omitted from the primary UI while concise pending progress and evidence citations remain. The composer now offers server-allowlisted model and reasoning-effort selection with additive v1 request/response fields and persisted session/message/turn execution metadata. A dedicated ChatGPT settings dialog exposes only safe connection status and gives administrators one bounded start/cancel device-login flow through the official Codex app-server token-free account APIs; only `https://auth.openai.com/codex/device` and the short-lived user code reach the browser. The integration resolves an explicit `codex` executable, the service `PATH`, or `~/.local/bin/codex`, forces a file credential store under the isolated `.local/soc-agent/codex` `CODEX_HOME`, and never reads or migrates global Codex credentials/configuration such as `~/.codex/auth.json` or Pi credentials, so a fresh SIEM login is required. App-server owns login and refresh; the bounded/redacted no-tools `chatgpt.com/backend-api/codex/responses` compatibility adapter reads only the isolated credential just in time and uses the current Codex request controls without the unsupported `max_output_tokens` field because app-server turns cannot guarantee tool-free execution. Each recent chat now has a direct accessible `×` delete control with no clipped menu or intermediate confirmation prompt, while the server retains its explicit deletion flag and one-shot audit turns. Explicit subscription OAuth-file, API-key, and delegated bearer modes remain advanced fallbacks with no implicit auth-file discovery. Native and advanced provider boundaries reject linked credential ancestors, require exact official no-redirect endpoints, cap provider/token response bytes, require a successful terminal streaming event, and bind interactive credential mutations to an administrator browser session. The release preserves existing `/api/v1` routes and `contracts/v1` compatibility and uses an additive database migration with no backfill requirement.
- **MINOR 1.9.0:** Added a disabled-by-default `Journal.IncludeAccessibleUserJournals` scope that can collect from all local systemd journals already readable by the existing non-root service identity while independently verifying mandatory system-journal visibility. Scope transitions preserve the durable cursor, persist and recover honestly from invalid cursors without claiming historical backfill, expose bounded scope/visibility health, and invalidate the passive-L3 and L4 approval hashes. The expansion keeps fixed `journalctl` argument sets, existing bounds/redaction and public-data rules, and performs no root, group, ACL, capability, journal-retention, producer, API, schema, or database change.
- **PATCH 1.8.1:** Fixed Linux L3 heartbeat delivery after passive host-metrics collection by deterministically bounding the final merged source-health detail map to the existing portable-v1 32-entry contract while retaining all runtime-owned interpretation fields first. This reliability fix prevents a valid local L3 baseline from being stranded behind repeated heartbeat validation failures; it preserves `/api/v1` and `contracts/v1`, requires no database migration, and does not broaden collection or host privileges.
- **MINOR 1.8.0:** Added a disabled-by-default, exact-plan-and-baseline-approval-gated Linux L4 path with child-complete policy/posture drift evidence, full-window agent CPU/RSS/write/queue SLO evidence, fixed journal classification for declared specialized host roles, persisted quiet-source evidence, strict server-side L1-L4 evaluation/freshness, and canonical role/L2 detection continuity. Durable sequence handling now records bounded rejection/poison gaps without head-of-line deadlock or sequence leaps. The release also adds a self-contained x64/ARM64 publisher, fail-closed lifecycle/identity/input checks, staged no-restart upgrades, synthetic tests, web guidance, and a private-VM deployment/acceptance runbook. Generic hosts declare a supported general role while specialized hosts must resolve and satisfy every applicable role source. This additive release preserves `/api/v1` and `contracts/v1`, requires no database migration, performs no host-policy/audit/eBPF/firewall/authentication mutation, and does not claim the still-outstanding private VM canary or soak evidence.
- Reworked the public README around the project's actual work-in-progress status, implemented versus approval-gated capabilities, known gaps, safe local evaluation, and contributor entry points; added the standard MIT License for project-owned source and documentation. This documentation and legal-metadata update does not alter runtime artifacts, APIs, schemas, or the project version.
- **MINOR 1.7.0:** Added a disabled-by-default, exact-plan-approval-gated Linux L3 passive telemetry pack for bounded procfs process and TCP/UDP socket snapshot differences plus aggregate host-behaviour metrics. The uplift includes durable sequence/source-health semantics, pressure and visibility-gap reporting, common credential-pattern redaction for sensitive process text, portable nested process/network/file search and role redaction, exact Linux L3 coverage evaluation, and prerequisite-aware process, listener, and severe-resource-pressure detections. Repository publication checks now validate prospective Git index paths and text blobs without traversing ignored private data. The release is additive, preserves `/api/v1` and `contracts/v1` compatibility, requires no database migration or backfill, performs no audit/eBPF/kernel/firewall/authentication changes, and keeps live L2/L3 activation plus any agent service restart behind an explicit operator-approved canary.
- Hardened request and detection integrity by rejecting batch-local duplicate event IDs for every source, conflicting portable flattened/nested telemetry concepts, non-canonical impersonations of known Linux catalog identities, and portable freshness/checkpoint timestamps beyond five minutes of receive-time skew; the new checks do not narrow legacy Windows envelope fields. Detection retry evaluates canonical stored rows for accepted and verified-duplicate acknowledgements, source snapshots explicitly mark omitted rows missing, passive freshness expires after two hours, recovered cumulative gap/drop counters remain historical, saved-search filters follow role redaction, configured ingest batches are capped at 500, and coalesced alert evidence is transactionally capped at 128 with explicit bound metadata and conservative confidence rollup.
- **PATCH 1.6.2:** Removed the stale-agent cleanup card and role-replacement notice from the Assets page while retaining registration filters and the guarded compatibility handler.
- **PATCH 1.6.1:** Refreshed the web console with a cooler slate-and-blue dark palette, centralized semantic background, border, text, accent, status, selection, and focus tokens, and retained accessible status-text contrast across operator workflows.
- **MINOR 1.6.0:** Refreshed the operator console shell and security overview around the current SIEM capabilities, and added an authenticated, role-scoped Streamable HTTP MCP endpoint at `/mcp`. The MCP surface provides bounded read-only tools and resources for events, alerts, cases, assets, source health, telemetry coverage, detections, inventory, and investigation graphs; includes evidence-led analyst prompts; records secret-safe audit metadata; and keeps detection improvements proposal-only. Public documentation, examples, local helper scripts, and repository-safety checks were also hardened to keep credentials, private telemetry, workstation paths, and operator-specific topology out of tracked content. The release is additive, preserves `/api/v1` and `contracts/v1` compatibility, and does not authorize endpoint or SIEM configuration mutation through MCP.
- Hardened MCP detail reads with database-side per-collection bounds and explicit truncation metadata for cases, source health, and investigation graphs; sanitized exact-record audit targets; best-effort secret-pattern redaction for admin-only endpoint inventory; and public-repository guards for local MCP credentials and generated test reports.
- Hardened the refreshed console and review APIs by preserving alert IDs across Razor POST actions, rendering version labels correctly, keeping viewer global search metadata-only, fixing filtered PostgreSQL event search and event-detail SQL composition, accepting streamed retention requests, and retaining append-only alert evidence during synthetic cleanup.
- Fixed Linux foreground-agent validation defects so enrollment preserves the complete self-integrity configuration, unobserved sources report null rather than default timestamps, synchronous native file handles are read safely by the bounded inventory collector, and Linux detection alerts persist affected-entity metadata correctly.
- Added a Linux local-host rollout validation runbook with a sanitized aggregate-result template, private-evidence handling, L1 24-hour and L1+L2 seven-day soak gates, authorization-aware recovery checks, L3 opt-in guardrails, and bounded rollback/cleanup guidance while preserving `/api/v1` and `contracts/v1` compatibility.
- Added real-application PostgreSQL-backed Playwright release gates for browser, WCAG-oriented accessibility, security/protected-field, responsive, and performance validation with deterministic synthetic seed/cleanup scripts, ignored `.local/release-gates/` artifacts, dependency documentation, repository-safety canaries, and release-readiness/runbook guidance. This is tests/tooling/docs-only and preserves `/api/v1` and `contracts/v1` compatibility.
- **MINOR 1.5.0:** Added mature bounded event exploration with strict structured global/event filters, cursor pagination, UTC timeline buckets with explicit host-local metadata, saved searches with owner/visibility/version/audit semantics, configurable Razor result columns, normalized detail/entity pivots, admin-only confirmation-gated audited CSV export with spreadsheet-injection protection, generalized Windows/Linux asset and host-detail workflows with platform/freshness/coverage/source/pressure/gap/capacity filtering, explicit source applicability and health guidance, queue/resource/source rate/lag/checkpoint plus retention/capacity visibility, mature alert triage and case-management workflows, detection rule management/prerequisite visibility, bounded accessible dashboards with saved owner/visibility/version layouts, and admin-only operator/session/source/retention/capacity/audit review with validated audited configuration mutations. The release uses additive `/api/v1` and `contracts/v1` changes plus lexical migrations, preserves v1 compatibility and role redaction, rejects arbitrary code/secret-shaped values, and does not enable L3 collection or mutate endpoint host policy.
- **MINOR 1.4.0:** Replaced the active Razor console shell and styling with a cohesive accessible Challenger SIEM design system, role-aware IA navigation, bounded global event-search affordance, reusable status/state/table/filter/breadcrumb/tabs/pagination patterns, CSP-compatible owned JavaScript assets, representative core-page cutover, updated web tests/docs, and a backward-compatible operator-visible UI release while preserving `/api/v1` and `contracts/v1` compatibility.
- **MINOR 1.3.0:** Added a disabled-by-default explicit-opt-in Linux L3 self-integrity snapshot collector, safe managed telemetry retention with a 30-day target and hard 100 GiB ceiling, and prerequisite-aware Linux server-side detections with exact rule/evidence metadata; the release preserves `/api/v1` and `contracts/v1` compatibility, keeps audit/eBPF/broad live file monitoring deferred, scopes retention deletion to managed telemetry only, and adds synthetic validation plus operator documentation.
- **MINOR 1.2.0:** Added bounded v1 agent/source/queue/resource/storage observability with timestamped resource metrics, queue bytes/pressure/send/backoff/recovery state, poison/drop counters, per-source rate/lag/silence/gap/permission transitions, persisted coverage/API exposure, and 100 GiB managed-storage capacity warnings at 70/85/95% while preserving existing Windows payload compatibility.
- Documented the optional Linux L3 telemetry ADR: audit and eBPF remain deferred, broad/live file-integrity approaches are not selected, and only a future snapshot-based agent self-integrity design candidate is adopted; no audit, eBPF, file-integrity collector, runtime dependency, or host-policy mutation is added.
- **MINOR 1.1.0:** Added an opt-in Linux L2 journald security source pack with structured-first normalization for login/session, SSH, sudo/su, cron/timers, package, firewall, kernel/security-module, service-change, and agent/log-tamper activity; first-class requirement/applicability/evidence health metadata; platform-aware server coverage and portable package search; and bounded synthetic fixtures and performance guards. L1 remains the default while the private seven-day L1+L2 host soak remains a rollout gate; no audit, eBPF, or file-integrity collector is included.
- **MAJOR 1.0.0:** Replaced shared production review-token operator authentication with database-backed operator identities. Existing review-token API clients and `Auth:ReviewToken` browser login configuration require operator bootstrap and new per-operator credentials. Endpoint-agent enrollment/ingest/heartbeat contracts remain compatible under `/api/v1`.
- Added passive cursor-based Linux L1 system-journal collection for kernel, boot, systemd service, authentication, and core-system records with deterministic v1 IDs, bounded redaction/normalization, durable queue-before-checkpoint and acknowledgement-before-delete semantics, explicit source/gap/permission/pressure health, and synthetic restart/replay/failure/benchmark coverage.
- Replaced the production operator review credential with PostgreSQL-backed operator identities, exact viewer/analyst/detection-engineer/admin RBAC, hardened revocable sessions, CSRF-safe mutations, lockout and credential lifecycle controls, role-aware sensitive-field redaction, a secure local bootstrap/recovery command, and append-only secret-safe security audit events while preserving endpoint-agent authentication and `/api/v1` agent contracts.

- Added bounded read-only Linux host and security-posture inventory snapshots for host/kernel identity, users/groups, services/units/timers, packages/available updates, interfaces/listeners, mounts, firewall, SSH, mandatory access controls, Secure Boot, and observable agent file-permission/fingerprint posture, with explicit collection states, fixed source policies, independent scheduling, and deterministic payload/item limits while preserving the generic v1 inventory contracts.

- Added a first-class .NET 8 Linux agent foundation using Agent.Core for enrollment, durable queueing, acknowledgement-aware recovery, heartbeat and bounded inventory transport, with a least-privilege systemd unit and preflight-first plan/install/upgrade/validate/uninstall workflows.

- Extracted a platform-neutral Agent.Core reliability library for the durable queue, v1 transport, acknowledgement/retry handling, deterministic identity, serialization, and configuration hashing while preserving the Windows agent collection/checkpoint semantics.

- Added numbered idempotent PostgreSQL migrations, multi-platform event persistence/search, portable-source indexes, and authenticated managed event-storage accounting while preserving Windows v1 rows and semantics.

- Added backward-compatible v1 cross-platform telemetry contracts with Linux-native journal/audit sources and platform-neutral Windows-or-Linux inventory-diff/agent-health sources, including explicit source/checkpoint/deduplication/applicability/health metadata, bounded structured concepts and raw-data handling, old-valid Windows boundary regressions, synthetic schema/golden validation, and an honest storage-migration boundary while preserving the exact legacy Windows Event Log payload allowances and semantics.
- Added host timezone metadata across registration, heartbeat, source-health, inventory, event ingest/search, storage, schemas, and web review displays so host-scoped timelines default to clearly labelled endpoint-local time while UTC remains canonical for filtering and correlation.
- Added a full-capability Windows agent installer workflow with plan/install/upgrade/repair/validate/uninstall modes, guarded Windows prerequisite configuration, Microsoft-signed Sysmon management, a versioned Sysmon L3 profile, source-manifest prerequisite/event-family validation metadata, and Sysmon profile hash reporting in source health.
- Added agent-scoped Windows telemetry coverage validation with expected-source matrix overlays, recent event counts, agent-uploaded bounded inventory/audit-policy snapshots, detection prerequisite status, a `/api/v1/telemetry-coverage` review endpoint, expanded host coverage web detail, and clearer missing-vs-error source status handling for readable empty Windows logs.
- Added a guarded fresh-start reset script and runbook for disposable local test environments with dry-run default, local target safety checks, confirmation-gated database reset, and opt-in local artifact cleanup.
- Added safe Markdown rendering for `/soc-agent` assistant chat output in persisted and live messages while keeping operator messages plain text and blocking unsafe links/raw HTML.
- Fixed `/soc-agent` chat auto-follow scrolling so live conversation updates keep the latest message above the sticky composer while still respecting manual scroll-away behavior.
- Fixed the `/soc-agent` live workspace so normal connected/local provider states use the compact title pill instead of a large inline status box, activity cards fit long tool names, and no-reload sends show immediate operator/pending progress while live events stream.
- Added authenticated `soc-agent` chat-session deletion from the web workspace and v1 review API, with active-run conflict handling and message cascade semantics.
- Expanded the `/soc-agent` workspace width, widened the conversation column, moved provider setup into compact status/notice surfaces, and kept the right rail focused on live tool activity.
- Fixed `/soc-agent` auto-follow scrolling to target the active chat thread sentinel instead of jumping to the document bottom on initial page load.
- Added a local platform lifecycle helper for starting, stopping, restarting, and checking the Challenger SIEM API/web console process without printing secrets.
- Updated the `/soc-agent` chat workspace to use browser/page-level scrolling instead of nested scroll panels while preserving live send, activity, provider status, and scroll-to-latest behavior.
- Refined the `/soc-agent` chat workspace with hidden manual agent context, a non-editable context chip, reliable activity-panel collapse/expand controls, user-controlled auto-follow scrolling, and Ctrl/Cmd+Enter send support.
- Added a live `soc-agent` workspace with same-origin event-stream run transport, no-reload chat sends, tool/progress events, cancellation, reconnect recovery, compact provider/activity panels, and docs/tests for the additive browser workflow.
- Added ChatGPT Responses support for `soc-agent` delegated subscription credential mode with bounded/redacted provider requests and fail-closed endpoint validation.
- Added server-side delegated auth-file reuse for `soc-agent` subscription OAuth mode without exposing provider credentials to the browser.
- Added a server-mediated ChatGPT subscription OAuth connect flow for `soc-agent` so authenticated operators can start official authorization-code/PKCE setup from the web console while credentials stay server-side.
- Added ChatGPT subscription OAuth as the primary external `soc-agent` provider setup path with safe auth-file parsing, official audience/scope/endpoint validation, near-expiry refresh, additive status metadata, web setup guidance, docs, and synthetic test coverage.
- Added opt-in delegated auth-file support for `soc-agent` OpenAI provider mode with safe local/secret path validation, official API audience checks, token-expiry status metadata, provider credential selection, redacted web/API status, docs, and synthetic test coverage.
- Added an optional official OpenAI Chat Completions-backed `soc-agent` provider path with server-side credential handling, safe status/error mapping, bounded redacted prompts, local fallback, and web/API documentation.
- Fixed stale-agent cleanup redirects in the web console so success and validation paths return safely to agent inventory with filters preserved.
- Expanded guarded synthetic-data cleanup to cover agent-linked `soc-agent` turns, sessions/messages, investigation graphs/proposals/audit, explicit synthetic chat/graph selectors, and web-smoke `soc-agent` chat cleanup.
- Revamped the web review console with a cohesive dark operator shell, active navigation, accessible focus/skip-link behavior, responsive cards/tables, visible filter/result state, bounded pagination for list workflows, and updated web validation/docs.
- Refreshed the public documentation into a wiki-style docs set with a concise README, operator/contributor/troubleshooting guides, sanitized web-console screenshots, and a documentation-maintenance checklist.
- Added a platform capability foundation catalog and authenticated API covering SPEC-GAP-001 through SPEC-GAP-019 with documented implementation guardrails and contracts.
- Added operator-managed investigation graphs with v1 APIs, PostgreSQL storage, web workflows, bounded `soc-agent` graph context, and approval-gated graph proposals.
- Added a guarded synthetic web/API test-data cleanup script with dry-run defaults, explicit execution confirmation, smoke-script cleanup hooks, docs, and integration coverage.
- Added a persistent `soc-agent` chat workspace with provider status/connect UX, additive chat APIs, bounded session/message persistence, and official ChatGPT/OpenAI setup guardrails.
- Added authenticated stale-agent cleanup controls that retire inactive registrations from default dashboard/inventory views without deleting telemetry.
- Added an authenticated local `soc-agent` web/API workspace with SIEM-aware tool orchestration, citations, bounded audit persistence, and current coverage/event/alert/detection/inventory context.
- Added a Windows host full-coverage foundation with source-health contracts/storage/API/web review, coverage and inventory models, parser/detection catalogs, alert and detection skeletons, validation runbooks, and role-pack designs.
- Documented the planned `soc-agent` web-console harness, official provider-auth guardrails, tool/approval model, and phased roadmap.
- Added a comprehensive Windows host full-coverage SIEM target specification and a bite-sized GitHub issue backlog covering sources, telemetry requirements, normalization, health, detections, validation, and roadmap.
- Added startup validation for required database, enrollment-token, and review-token configuration with non-secret error messages.
- Added PostgreSQL schema apply/validation scripts plus opt-in PostgreSQL integration coverage for registration, token rotation, ingest/dedup/search, heartbeat persistence, ingestion error recording, and web console data display.
- Persisted authenticated ingest validation failures to `ingestion_errors` with bounded secret-safe context.
- Added additive v1 ingest acknowledgement event-id arrays so agents can delete only accepted/duplicate queued events after partial acknowledgements.
- Added Windows agent first-run enrollment, per-agent token persistence, bounded retry/backoff, queue attempt tracking, poison-event quarantine, queue limit warnings, and unit coverage for normalization/queue/state behavior.
- Hardened Windows agent service install documentation and scripts for LocalSystem MVP service account, Administrators/SYSTEM-only config/data ACLs, Security log permission validation, and safe generated config handling.
- Added non-Docker web smoke, operator runbooks, TLS deployment guidance, Windows lab E2E guidance, packaging validation notes, and MVP release readiness checklist.
- Added a server-hosted web review console with review-token login, dashboard metrics, agent inventory, event search, event detail, and system/about pages.
- Ensured web console static assets are also covered by production HTTPS enforcement.
- Added web authentication/session tests and web review console operator documentation.
- Documented a configurable, operator-approved WinRM lab and endpoint-to-server callback workflow for E2E validation.
- Hardened ignore and repository-safety guidance for local automation state.
- Added the project versioning workflow for managed changes.
- Centralized project version metadata in `VERSION` and .NET assembly metadata.

## 0.1.0 - 2026-07-04

- Established the MVP baseline for the Windows agent, ingestion API, contracts, and local development docs.
